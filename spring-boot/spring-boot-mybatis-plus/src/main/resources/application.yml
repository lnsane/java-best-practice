server:
  port: 9093
spring:
  flyway:
    enabled: false
  datasource:
    database-address: jdbc:mysql://localhost:3306/
    database-name: test
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${spring.datasource.database-address}${spring.datasource.database-name}?useUnicode=true&characterEncoding=utf8
mybatis-plus:
  type-enums-package: com.best.spring.boot.mybatis.plus.enums