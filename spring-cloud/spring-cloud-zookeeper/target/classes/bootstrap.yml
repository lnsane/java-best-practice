server:
  port: 8419
spring:
  application:
    name: spring-boot-zookeeper-demo
  cloud:
    zookeeper:
      connect-string: localhost:2181
      # 服务发现
      discovery:
        # 注册自身
        register: true
