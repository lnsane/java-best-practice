microservices:
  mode: "false"
feign:
  client:
    config:
      default:
        connectTimeout: 15000
        readTimeout: 15000
  hystrix:
    enabled: true

hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 6000