spring:
  rabbitmq:
    password: guest
    username: guest
    host: localhost
  datasource:
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf8
  redis:
    database: 1
    host: localhost
    port: 6379
    redisson:
      file: classpath:redisson-config.json
server:
  port: 12082